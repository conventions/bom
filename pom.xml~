<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.conventionsframework</groupId>
    <artifactId>bundle</artifactId>
    <version>0.9.7-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>conventions-bundle</name>
  
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <myfaces.codi.version>1.0.5</myfaces.codi.version>
        <hibernate.version>4.1.5.Final</hibernate.version>
        <primefaces.version>3.4-SNAPSHOT</primefaces.version>
        <prime-ext.version>0.6.0-SNAPSHOT</prime-ext.version>
    </properties>
    <parent>
        <groupId>org.sonatype.oss</groupId>
        <artifactId>oss-parent</artifactId>
        <version>7</version>
        <relativePath />
    </parent>
    <licenses>
        <license>
            <name>Apache 2</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
            <comments>A business-friendly OSS license</comments>
        </license>
    </licenses>
     <scm>
        <connection>scm:git:git://github.com/conventions/bundle-glassfish.git</connection>
        <developerConnection>scm:git:git@github.com:conventions/bundle-glassfish.git</developerConnection>
        <url>git@github.com:conventions/bundle-glassfish.git</url>
    </scm>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>package-all</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/assembler.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
    
    </dependencies>

    <properties>
        <primefaces.theme.version>1.0.6</primefaces.theme.version>    
    </properties>


</project>
